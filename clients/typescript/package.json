{
  "name": "public-business-data-client-dev",
  "private": true,
  "description": "Manifiesto de desarrollo del cliente: build, tests y typecheck. El paquete que se instala es el del raíz del repositorio.",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
