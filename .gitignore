# Credenciales: config.toml lleva el token de GitHub cuando no se usa SSM
config.toml*
config.*.toml*

!config.example.toml

# Binarios de build del updater
updater/bootstrap
updater/*.zip

node_modules/

# Generados por SvelteKit: .svelte-kit en cada `sync`, y web/build es el intermedio del que
# scripts/publish.mjs copia index.html y _app a docs/. Lo que se publica es docs/, y ése sí va.
.svelte-kit/
web/build/

# Basura del sistema de archivos
.DS_Store
Thumbs.db
